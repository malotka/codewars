def arithmetics(num1, num2, operand):

    if operand == "add":
        return num1 + num2
    if operand == "subtract":
        return num1 - num2
    if operand == "multiply":
        return num1 * num2
    if operand == "divide":
        return num1 / num2